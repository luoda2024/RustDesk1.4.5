[package]
name = "libxdo-sys"
version = "0.11.0"
authors = ["Stub Implementation"]
edition = "2018"
description = "Stub implementation of libxdo-sys for systems without libxdo"
license = "MIT"

[lib]
name = "libxdo_sys"
crate-type = ["lib"]

[dependencies]
libc = "0.2"
