name: Build Windows EXE

on:
  push:
    branches:
      - master
      - luoda-custom
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: true
      upload-tag: "luoda"
